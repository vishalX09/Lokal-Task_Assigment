<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="DialogFragmentStyle" parent="Theme.AppCompat.Dialog">
        <!-- Customize the style attributes as needed -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@style/DialogFragmentAnimation</item>
        <item name="android:backgroundDimAmount">0.6</item> <!-- Adjust the dim amount as needed -->
    </style>

    <style name="DialogFragmentAnimation">
        <!-- Add your animation style if needed -->
    </style>


</resources>